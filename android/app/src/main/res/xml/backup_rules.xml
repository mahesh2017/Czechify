<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Learner data and credentials are restored only through Czechify's
         authenticated sync/export paths, never opaque platform backup. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
